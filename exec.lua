pcall(function()
  game.StarterGui:SetCore("SendNotification",{Title = "Table",Text = "This script is heavily work-in-progress, it won't do anything. Check discord for updates.", Duration = 10})
  setclipboard("No discord server yet.")
end)
